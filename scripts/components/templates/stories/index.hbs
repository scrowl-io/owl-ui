import * as React from 'react';
import { {{{cap}}} } from '../index';
import { {{{cap}}}{{{optionPas}}}Props } from '../{{{optionPas}}}.types';

import { variant } from './{{{optionPas}}}-variant.stories';
import { pxScale } from './{{{optionPas}}}-px-scale.stories';
import { theme } from './{{{optionPas}}}-theme.stories';

export const {{{optionPas}}} = (args: {{{cap}}}{{{optionPas}}}Props) => <{{{cap}}} {...args}>Hello World</{{{cap}}}>;

{{{optionPas}}}.args = {
  variant: variant.defaultValue,
  pxScale: pxScale.defaultValue,
  theme: theme.defaultValue,
};

{{{optionPas}}}.argTypes = {
  variant,
  pxScale,
  theme,
};
