import * as React from 'react'
import { component as {{{cap}}} } from '../index'

import { appearance } from './{{{optionCap}}}-appearance.stories'
import { size } from './{{{optionCap}}}-size.stories'
import { theme } from './{{{optionCap}}}-theme.stories'

export const {{{optionCap}}} = (args) => <{{{cap}}} {...args}>Hello World</{{{cap}}}>

{{{optionCap}}}.args = {
    appearance: 'Primary',
    size: 'Lg',
    theme: 'Default'
}

{{{optionCap}}}.argTypes = {
    appearance,
    size,
    theme
}