import * as React from 'react'
import { {{{cap}}}Props } from './{{{cap}}}.types'
import * as styles from './styles.module.scss'

export const {{{cap}}} = (props: {{{cap}}}Props) => {
    const { children } = props

    let appearance = props.appearance
    let size = props.size

    if (appearance === undefined) {
        appearance = 'primary'
    }

    if (size === undefined) {
        size = 'medium'
    }

    return <div
        className={[
            styles.default.{{{name}}},
            styles.default[
                `{{{name}}}_${props.theme ? (appearance + '--' + props.theme) : appearance}`
            ],
            styles.default[`{{{name}}}--${size}`]
        ].join(' ')}
    >{children}</div>
}

export default {{{cap}}};