import * as {{{optionCap}}}{{{cap}}} from './{{{optionCap}}}/index'

export const {{{optionCap}}} = {{{optionCap}}}{{{cap}}}.component
export const {{{optionCap}}}Types = {{{optionCap}}}{{{cap}}}.types

export default {
    {{{optionCap}}},
    {{{optionCap}}}Types
}