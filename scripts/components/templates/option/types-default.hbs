import React from 'react';
import * as bs from 'react-bootstrap';

export interface {{{cap}}}{{{optionPas}}}Commons {
  theme?: 'Default' | 'Dark';
  size?: 'Sm' | 'Md' | 'Lg';
  prefix?: string;
};

export type {{{cap}}}{{{optionPas}}}Props = Partial<{{{cap}}}{{{optionPas}}}Commons> & React.AllHTMLAttributes<HTMLDivElement>;
