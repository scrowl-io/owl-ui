import * as React from 'react'
import { Component as Default } from '../Default/Default'
import { {{{cap}}}{{{optionCap}}}Props } from './{{{optionCap}}}.types'
import * as styles from './styles.module.scss'

const baseClass = 'owlui{{{cap}}}'

export const Component = (props: {{{cap}}}{{{optionCap}}}Props) => {
  const { children } = props
  const localProps = Object.assign({}, props)
  
  localProps.className = localProps.hasOwnProperty('className') ? `${styles[baseClass]} ${localProps.className}` : styles[baseClass]

  return <Default
    {...localProps}
  >{children}</Default>
}

export default {
    Component
};