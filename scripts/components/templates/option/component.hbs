import * as React from 'react'
import { Component as Default } from '../Default/Default'
import { {{{cap}}}{{{optionPas}}}Props } from './{{{optionPas}}}.types'
import * as styleMod from './styles.module.scss';
import { createLocalProps } from '../../../../lib/src/utils';

export const Component = (props: {{{cap}}}{{{optionPas}}}Props) => {
  const baseClass = '{{{name}}}{{{optionPas}}}';
  const { children } = props;
  const prefix = props.prefix || '';

  const locals = createLocalProps(
    props,
    {
      module: styleMod,
      classes: {
        base: baseClass,
        prefix: prefix,
        optionals: [
          {
            fields: ['theme'],
            value: 'theme',
          },
          {
            fields: ['pxScale'],
            value: 'PxScale',
          },
        ],
      },
    },
    ['prefix', 'theme', 'pxScale']
  );

  return <Default {...locals}>{children}</Default>;
}

export default {
  Component,
};
