{{#with . as | data |}}
{{#each data.components as | comp |}}
import * as {{comp.cap}} from '{{comp.pkg}}';
{{/each}}
import './_index.scss';

{{#each data.components as | comp |}}
export const {{comp.name}} = {{comp.cap}};
{{/each}}

export default {
{{#each data.components as | comp |}}
  {{comp.name}},
{{/each}}
};
{{/with}}