import * as React from 'react'
import { {{{cap}}} } from '../index'

import { appearance } from './{{{cap}}}-appearance.stories'
import { size } from './{{{cap}}}-size.stories'
import { theme } from './{{{cap}}}-theme.stories'

export const Default = (args) => <{{{cap}}} {...args}>Hello World</{{{cap}}}>

Default.args = {
    appearance: 'primary',
    size: 'large',
    theme: undefined
}

Default.argTypes = {
    appearance,
    size,
    theme
}