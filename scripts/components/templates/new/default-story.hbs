import * as React from 'react'
import { Meta } from '@storybook/react'
import { {{{cap}}} } from './{{{cap}}}'
import descriptionMd from './Description.md'

export { Default } from './stories/{{{cap}}}-default.stories'

export default {
    title: 'Components/{{{cap}}}',
    component: {{{cap}}},
    parameters: {
        docs: {
            description: {
                component: [
                    descriptionMd
                ].join('/n')
            }
        }
    },
    decorators: [
        {{{{raw}}}}
        Story => (
            <div style={{
                display: 'flex',
                justifyContent: 'space-evenly'
            }}>
                <Story />
            </div>
        )
        {{{{/raw}}}}
    ]
} as Meta