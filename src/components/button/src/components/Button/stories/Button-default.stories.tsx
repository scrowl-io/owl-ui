import * as React from 'react'
import { Button } from '../index'

export const Default = () => {
    return <Button
                label="Hello World"
                size="medium"
                appearance="primary"
            />
}