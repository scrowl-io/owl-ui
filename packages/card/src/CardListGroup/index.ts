import { Component } from './CardListGroup';
import * as Types from './CardListGroup.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
