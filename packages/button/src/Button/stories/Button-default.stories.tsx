import * as React from 'react'
import { Button } from '../index'

export const Default = () => {
    return <Button
                size="large"
                appearance="primary"
            >Hello World</Button>
}