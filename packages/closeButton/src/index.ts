import * as DefaultCloseButton from './Default/index';

export const Default = DefaultCloseButton.component;
export const DefaultTypes = DefaultCloseButton.types;

export default {
  Default,
  DefaultTypes,
};
