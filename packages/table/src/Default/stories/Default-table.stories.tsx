export const hover = {
  control: {
    type: 'boolean',
  },
  defaultValue: true,
};

export const bordered = {
  control: {
    type: 'boolean',
  },
  defaultValue: true,
};

export const striped = {
  control: {
    type: 'boolean',
  },
  defaultValue: true,
};

export default {
  hover,
  bordered,
  striped,
};
