import { Component } from './KeyboardReturn';
import * as Types from './KeyboardReturn.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
