import { Component } from './FeaturedPlayList';
import * as Types from './FeaturedPlayList.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
