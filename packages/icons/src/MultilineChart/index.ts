import { Component } from './MultilineChart';
import * as Types from './MultilineChart.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
