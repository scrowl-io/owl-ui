import { Component } from './FullscreenExit';
import * as Types from './FullscreenExit.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
