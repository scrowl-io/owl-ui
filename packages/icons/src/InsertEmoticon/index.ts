import { Component } from './InsertEmoticon';
import * as Types from './InsertEmoticon.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
