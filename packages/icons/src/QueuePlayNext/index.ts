import { Component } from './QueuePlayNext';
import * as Types from './QueuePlayNext.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
