import { Component } from './DeveloperMode';
import * as Types from './DeveloperMode.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
