import { Component } from './FiberSmartRecord';
import * as Types from './FiberSmartRecord.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
