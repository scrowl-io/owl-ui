import { Component } from './CompareArrows';
import * as Types from './CompareArrows.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
