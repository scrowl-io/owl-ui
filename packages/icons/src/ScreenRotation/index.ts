import { Component } from './ScreenRotation';
import * as Types from './ScreenRotation.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
