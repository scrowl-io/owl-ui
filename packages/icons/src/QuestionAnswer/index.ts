import { Component } from './QuestionAnswer';
import * as Types from './QuestionAnswer.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
