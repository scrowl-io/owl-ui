import { Component } from './FavoriteBorder';
import * as Types from './FavoriteBorder.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
