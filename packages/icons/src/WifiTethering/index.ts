import { Component } from './WifiTethering';
import * as Types from './WifiTethering.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
