import { Component } from './AssistantPhoto';
import * as Types from './AssistantPhoto.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
