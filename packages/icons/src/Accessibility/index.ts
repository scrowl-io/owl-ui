import { Component } from './Accessibility';
import * as Types from './Accessibility.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
