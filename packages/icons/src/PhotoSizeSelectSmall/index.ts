import { Component } from './PhotoSizeSelectSmall';
import * as Types from './PhotoSizeSelectSmall.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
