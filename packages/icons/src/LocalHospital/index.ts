import { Component } from './LocalHospital';
import * as Types from './LocalHospital.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
