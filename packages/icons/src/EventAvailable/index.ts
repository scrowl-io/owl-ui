import { Component } from './EventAvailable';
import * as Types from './EventAvailable.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
