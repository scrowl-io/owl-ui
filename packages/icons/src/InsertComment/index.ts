import { Component } from './InsertComment';
import * as Types from './InsertComment.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
