import { Component } from './PermDeviceInformation';
import * as Types from './PermDeviceInformation.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
