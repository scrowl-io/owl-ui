import { Component } from './VerticalAlignCenter';
import * as Types from './VerticalAlignCenter.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
