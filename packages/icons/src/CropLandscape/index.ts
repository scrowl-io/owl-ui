import { Component } from './CropLandscape';
import * as Types from './CropLandscape.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
