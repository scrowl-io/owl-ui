import { Component } from './FolderSpecial';
import * as Types from './FolderSpecial.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
