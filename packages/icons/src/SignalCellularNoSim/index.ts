import { Component } from './SignalCellularNoSim';
import * as Types from './SignalCellularNoSim.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
