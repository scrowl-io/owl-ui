import { Component } from './VerticalAlignBottom';
import * as Types from './VerticalAlignBottom.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
