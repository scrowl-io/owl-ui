import { Component } from './ImageAspectRatio';
import * as Types from './ImageAspectRatio.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
