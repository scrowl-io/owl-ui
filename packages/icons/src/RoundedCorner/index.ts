import { Component } from './RoundedCorner';
import * as Types from './RoundedCorner.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
