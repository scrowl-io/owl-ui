import { Component } from './DeleteForever';
import * as Types from './DeleteForever.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
