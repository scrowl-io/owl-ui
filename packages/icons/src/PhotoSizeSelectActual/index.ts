import { Component } from './PhotoSizeSelectActual';
import * as Types from './PhotoSizeSelectActual.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
