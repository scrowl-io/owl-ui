import { Component } from './LaptopChromebook';
import * as Types from './LaptopChromebook.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
