import { Component } from './NotInterested';
import * as Types from './NotInterested.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
