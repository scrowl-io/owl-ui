import { Component } from './SignalWifi4Bar';
import * as Types from './SignalWifi4Bar.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
