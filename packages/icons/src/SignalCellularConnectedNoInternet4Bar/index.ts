import { Component } from './SignalCellularConnectedNoInternet4Bar';
import * as Types from './SignalCellularConnectedNoInternet4Bar.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
