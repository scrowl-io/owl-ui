import { Component } from './FormatLineSpacing';
import * as Types from './FormatLineSpacing.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
