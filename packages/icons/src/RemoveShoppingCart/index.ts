import { Component } from './RemoveShoppingCart';
import * as Types from './RemoveShoppingCart.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
