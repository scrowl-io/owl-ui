import { Component } from './FreeBreakfast';
import * as Types from './FreeBreakfast.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
