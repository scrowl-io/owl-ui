import { Component } from './RadioButtonUnchecked';
import * as Types from './RadioButtonUnchecked.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
