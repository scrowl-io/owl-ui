import { Component } from './DeveloperBoard';
import * as Types from './DeveloperBoard.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
