import { Component } from './SettingsEthernet';
import * as Types from './SettingsEthernet.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
