import { Component } from './LocalGasStation';
import * as Types from './LocalGasStation.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
