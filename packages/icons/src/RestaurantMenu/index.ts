import { Component } from './RestaurantMenu';
import * as Types from './RestaurantMenu.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
