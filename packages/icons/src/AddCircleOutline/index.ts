import { Component } from './AddCircleOutline';
import * as Types from './AddCircleOutline.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
