import { Component } from './BrightnessMedium';
import * as Types from './BrightnessMedium.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
