import { Component } from './SettingsRemote';
import * as Types from './SettingsRemote.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
