import { Component } from './SettingsInputComponent';
import * as Types from './SettingsInputComponent.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
