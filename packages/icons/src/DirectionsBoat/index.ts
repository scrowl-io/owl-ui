import { Component } from './DirectionsBoat';
import * as Types from './DirectionsBoat.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
