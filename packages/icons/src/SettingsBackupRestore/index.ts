import { Component } from './SettingsBackupRestore';
import * as Types from './SettingsBackupRestore.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
