import { Component } from './SettingsPower';
import * as Types from './SettingsPower.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
