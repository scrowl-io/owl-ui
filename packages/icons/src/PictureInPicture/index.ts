import { Component } from './PictureInPicture';
import * as Types from './PictureInPicture.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
