import { Component } from './KeyboardArrowDown';
import * as Types from './KeyboardArrowDown.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
