import { Component } from './FormatAlignLeft';
import * as Types from './FormatAlignLeft.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
