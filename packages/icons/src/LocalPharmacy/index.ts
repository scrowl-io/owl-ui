import { Component } from './LocalPharmacy';
import * as Types from './LocalPharmacy.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
