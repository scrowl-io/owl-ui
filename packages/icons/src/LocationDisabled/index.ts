import { Component } from './LocationDisabled';
import * as Types from './LocationDisabled.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
