import { Component } from './ImportantDevices';
import * as Types from './ImportantDevices.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
