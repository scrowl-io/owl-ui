import { Component } from './FeaturedVideo';
import * as Types from './FeaturedVideo.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
