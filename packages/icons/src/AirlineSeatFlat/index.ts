import { Component } from './AirlineSeatFlat';
import * as Types from './AirlineSeatFlat.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
