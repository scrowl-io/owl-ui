import { Component } from './SettingsPhone';
import * as Types from './SettingsPhone.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
