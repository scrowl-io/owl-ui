import { Component } from './MovieCreation';
import * as Types from './MovieCreation.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
