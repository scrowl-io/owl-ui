import { Component } from './LaptopWindows';
import * as Types from './LaptopWindows.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
