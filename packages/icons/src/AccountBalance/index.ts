import { Component } from './AccountBalance';
import * as Types from './AccountBalance.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
