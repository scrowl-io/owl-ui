import { Component } from './ArrowDownward';
import * as Types from './ArrowDownward.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
