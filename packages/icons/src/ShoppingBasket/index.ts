import { Component } from './ShoppingBasket';
import * as Types from './ShoppingBasket.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
