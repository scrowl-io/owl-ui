import { Component } from './ArrowDropDown';
import * as Types from './ArrowDropDown.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
