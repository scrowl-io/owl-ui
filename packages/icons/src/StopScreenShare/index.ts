import { Component } from './StopScreenShare';
import * as Types from './StopScreenShare.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
