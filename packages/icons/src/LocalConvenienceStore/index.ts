import { Component } from './LocalConvenienceStore';
import * as Types from './LocalConvenienceStore.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
