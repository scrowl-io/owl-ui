import { Component } from './RadioButtonChecked';
import * as Types from './RadioButtonChecked.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
