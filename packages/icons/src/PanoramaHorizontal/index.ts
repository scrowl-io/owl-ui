import { Component } from './PanoramaHorizontal';
import * as Types from './PanoramaHorizontal.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
