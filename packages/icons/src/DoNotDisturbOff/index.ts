import { Component } from './DoNotDisturbOff';
import * as Types from './DoNotDisturbOff.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
