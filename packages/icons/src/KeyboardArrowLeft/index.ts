import { Component } from './KeyboardArrowLeft';
import * as Types from './KeyboardArrowLeft.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
