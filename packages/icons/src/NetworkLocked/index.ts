import { Component } from './NetworkLocked';
import * as Types from './NetworkLocked.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
