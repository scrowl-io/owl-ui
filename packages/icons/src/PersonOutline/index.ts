import { Component } from './PersonOutline';
import * as Types from './PersonOutline.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
