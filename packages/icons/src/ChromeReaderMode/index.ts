import { Component } from './ChromeReaderMode';
import * as Types from './ChromeReaderMode.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
