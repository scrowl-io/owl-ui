import { Component } from './RemoveCircleOutline';
import * as Types from './RemoveCircleOutline.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
