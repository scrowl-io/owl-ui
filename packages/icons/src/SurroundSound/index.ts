import { Component } from './SurroundSound';
import * as Types from './SurroundSound.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
