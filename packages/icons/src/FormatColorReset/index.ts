import { Component } from './FormatColorReset';
import * as Types from './FormatColorReset.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
