import { Component } from './KeyboardCapslock';
import * as Types from './KeyboardCapslock.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
