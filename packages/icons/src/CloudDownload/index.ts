import { Component } from './CloudDownload';
import * as Types from './CloudDownload.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
