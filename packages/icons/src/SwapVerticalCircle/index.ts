import { Component } from './SwapVerticalCircle';
import * as Types from './SwapVerticalCircle.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
