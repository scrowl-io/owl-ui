import { Component } from './PlaylistAddCheck';
import * as Types from './PlaylistAddCheck.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
