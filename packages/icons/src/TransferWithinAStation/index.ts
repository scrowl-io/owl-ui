import { Component } from './TransferWithinAStation';
import * as Types from './TransferWithinAStation.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
