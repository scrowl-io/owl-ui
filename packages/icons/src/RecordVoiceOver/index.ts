import { Component } from './RecordVoiceOver';
import * as Types from './RecordVoiceOver.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
