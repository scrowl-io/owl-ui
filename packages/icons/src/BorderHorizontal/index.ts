import { Component } from './BorderHorizontal';
import * as Types from './BorderHorizontal.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
