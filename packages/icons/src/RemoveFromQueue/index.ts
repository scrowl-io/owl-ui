import { Component } from './RemoveFromQueue';
import * as Types from './RemoveFromQueue.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
