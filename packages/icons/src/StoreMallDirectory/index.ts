import { Component } from './StoreMallDirectory';
import * as Types from './StoreMallDirectory.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
