import { Component } from './NotificationsPaused';
import * as Types from './NotificationsPaused.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
