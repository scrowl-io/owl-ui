import { Component } from './ImportContacts';
import * as Types from './ImportContacts.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
