import { Component } from './FilterVintage';
import * as Types from './FilterVintage.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
