import { Component } from './StayCurrentLandscape';
import * as Types from './StayCurrentLandscape.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
