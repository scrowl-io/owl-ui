import { Component } from './FlightTakeoff';
import * as Types from './FlightTakeoff.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
