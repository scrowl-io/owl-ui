import { Component } from './FormatListNumbered';
import * as Types from './FormatListNumbered.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
