import { Component } from './PermDataSetting';
import * as Types from './PermDataSetting.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
