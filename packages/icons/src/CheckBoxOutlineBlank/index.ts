import { Component } from './CheckBoxOutlineBlank';
import * as Types from './CheckBoxOutlineBlank.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
