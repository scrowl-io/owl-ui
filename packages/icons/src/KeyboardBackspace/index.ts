import { Component } from './KeyboardBackspace';
import * as Types from './KeyboardBackspace.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
