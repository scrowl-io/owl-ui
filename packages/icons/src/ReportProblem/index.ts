import { Component } from './ReportProblem';
import * as Types from './ReportProblem.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
