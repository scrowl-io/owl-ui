import { Component } from './ConfirmationNumber';
import * as Types from './ConfirmationNumber.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
