import { Component } from './NavigateBefore';
import * as Types from './NavigateBefore.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
