import { Component } from './LocalPrintshop';
import * as Types from './LocalPrintshop.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
