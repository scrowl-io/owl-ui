import { Component } from './ChangeHistory';
import * as Types from './ChangeHistory.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
