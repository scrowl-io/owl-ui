import { Component } from './InsertDriveFile';
import * as Types from './InsertDriveFile.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
