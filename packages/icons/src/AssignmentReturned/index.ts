import { Component } from './AssignmentReturned';
import * as Types from './AssignmentReturned.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
