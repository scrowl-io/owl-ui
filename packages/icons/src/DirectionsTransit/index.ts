import { Component } from './DirectionsTransit';
import * as Types from './DirectionsTransit.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
