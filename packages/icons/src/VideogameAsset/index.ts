import { Component } from './VideogameAsset';
import * as Types from './VideogameAsset.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
