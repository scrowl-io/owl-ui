import { Component } from './CastConnected';
import * as Types from './CastConnected.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
