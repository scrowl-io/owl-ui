import { Component } from './OpenInBrowser';
import * as Types from './OpenInBrowser.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
