import { Component } from './CreateNewFolder';
import * as Types from './CreateNewFolder.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
