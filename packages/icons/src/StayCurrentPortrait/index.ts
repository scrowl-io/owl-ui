import { Component } from './StayCurrentPortrait';
import * as Types from './StayCurrentPortrait.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
