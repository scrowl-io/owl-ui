import { Component } from './PeopleOutline';
import * as Types from './PeopleOutline.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
