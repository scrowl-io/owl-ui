import { Component } from './LocalShipping';
import * as Types from './LocalShipping.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
