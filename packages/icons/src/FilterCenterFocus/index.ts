import { Component } from './FilterCenterFocus';
import * as Types from './FilterCenterFocus.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
