import { Component } from './SupervisorAccount';
import * as Types from './SupervisorAccount.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
