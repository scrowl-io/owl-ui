import { Component } from './AddShoppingCart';
import * as Types from './AddShoppingCart.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
