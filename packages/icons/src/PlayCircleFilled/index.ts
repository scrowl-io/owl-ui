import { Component } from './PlayCircleFilled';
import * as Types from './PlayCircleFilled.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
