import { Component } from './CameraEnhance';
import * as Types from './CameraEnhance.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
