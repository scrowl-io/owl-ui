import { Component } from './AssignmentReturn';
import * as Types from './AssignmentReturn.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
