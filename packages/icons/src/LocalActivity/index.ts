import { Component } from './LocalActivity';
import * as Types from './LocalActivity.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
