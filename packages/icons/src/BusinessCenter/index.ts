import { Component } from './BusinessCenter';
import * as Types from './BusinessCenter.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
