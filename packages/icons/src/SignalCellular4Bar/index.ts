import { Component } from './SignalCellular4Bar';
import * as Types from './SignalCellular4Bar.types';

export const component = Component;
export const types = Types;

export default {
  component,
  types,
};
